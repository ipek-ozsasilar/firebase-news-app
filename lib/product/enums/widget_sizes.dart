//pagesizes dıyerek tum sızelerı bır enumda toplamak yerıne gruplandırarak kucuk parcalara bolduk

enum WidgetSizes{
  buttonNormal(56),
  iconNormal(24);

  final int value;
  const WidgetSizes(this.value);
}